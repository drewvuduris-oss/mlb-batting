# MLB Batting

A website for exploring MLB batting stats.

## Running locally

Open `index.html` in a browser, or serve the folder:

```
python -m http.server 8000
```

Then visit http://localhost:8000.
